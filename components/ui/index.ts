/**
 * UI Component Library
 * 
 * Shared, reusable UI components for Marshall's admin and public-facing pages
 */

export { ConfirmDialog } from './ConfirmDialog';

// Re-export types
export type { ConfirmDialogProps } from './ConfirmDialog';